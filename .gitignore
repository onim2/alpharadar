# ═══════════════════════════════════════════════════════
# 🔐 보안 — 절대 커밋 금지
# ═══════════════════════════════════════════════════════
.env
.env.*
!.env.example
.kis_token
*.token
secrets.yaml

# ═══════════════════════════════════════════════════════
# Python
# ═══════════════════════════════════════════════════════
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.pytest_cache/
.mypy_cache/

# ═══════════════════════════════════════════════════════
# 가상환경
# ═══════════════════════════════════════════════════════
venv/
env/
.venv/
ENV/

# ═══════════════════════════════════════════════════════
# OS / IDE
# ═══════════════════════════════════════════════════════
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
*.swo

# ═══════════════════════════════════════════════════════
# 백업
# ═══════════════════════════════════════════════════════
*.bak
*.bak_*
*.backup
*.orig
*~

# ═══════════════════════════════════════════════════════
# AlphaRadar 캐시 — 커밋할 것과 무시할 것 분리
# 중요: data/cache/, data/logs/ 디렉토리 자체는 무시 X.
#       부모 디렉토리가 ignore 되면 자식 파일은 ! 로도 unignore 불가.
# ═══════════════════════════════════════════════════════

# 일별 캐시 — 무시 (날짜별 생성, 크기 큼, GitHub Actions cache 로 보호)
data/cache/datalab_*.pkl
data/cache/news_*.pkl
data/cache/precompute_*.pkl
data/cache/news_titles_*.pkl
data/cache/news_items_*.pkl
data/cache/dart_items_*.pkl
data/cache/dart_titles_*.pkl
data/cache/universe_*.pkl
data/cache/pool_a_*.pkl
data/cache/results_*.pkl
data/cache/kis_investor_*.pkl

# 임시 파일
data/cache/*.pkl.tmp

# 로그 — 오래된/압축된 것만 무시
data/logs/*.log.old
data/logs/*.gz

# ─────────────────────────────────────────────────────
# 자동 커밋 대상 (위 패턴에 안 걸리므로 트래킹됨):
#   data/cache/dart_sector_map.pkl
#   data/cache/dart_sector_map_v2.pkl
#   data/cache/dart_corp_codes.pkl
#   data/scores_history.db
#   data/logs/scanner_*.log
# ─────────────────────────────────────────────────────

# 로컬 실행 산출물 — 러너에서 재생성되거나 로컬 전용
data/logs.zip
data/*.LOCAL*.log
data/cache/forward_returns.pkl
data/cache/price_cache_wr.pkl

# afterhours 재적재용 임시 산출물 (Actions 내부에서만 쓴다)
afterhours_rows.json
afterhours_*.csv
